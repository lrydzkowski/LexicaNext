namespace LexicaNext.Infrastructure.Db.Extensions;

public static class PostgresFunctions
{
    public static DateTime Timezone(string timeZone, DateTimeOffset value)
        => throw new NotSupportedException();
}
